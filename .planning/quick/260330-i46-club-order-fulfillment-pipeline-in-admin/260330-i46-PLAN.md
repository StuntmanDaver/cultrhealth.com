# Quick Task 260330-i46: Club Order Fulfillment Pipeline in Admin Dashboard

**Created:** 2026-03-30
**Status:** Ready for execution

## Goal
Add full lifecycle tracking for club orders in the admin dashboard: purchase → invoice sent → paid → shipped → fulfilled. Manual status updates since QB pay link automation is deferred.

## Tasks

### Task 1: Database Migration + DB Functions + Types
**Files:** `migrations/028_club_order_fulfillment.sql`, `lib/db.ts`, `lib/admin-types.ts`
**Action:**
- Create migration adding `paid_at`, `shipped_at`, `fulfilled_at`, `tracking_carrier`, `tracking_number`, `tracking_url` to `club_orders`
- Add `updateClubOrderStatus()` and `getClubOrderFulfillmentCounts()` to `lib/db.ts`
- Add `ClubOrderFulfillmentCounts` type to `lib/admin-types.ts`
**Verify:** Migration SQL is syntactically valid, types compile
**Done:** Migration file exists, DB functions exported, types exported

### Task 2: Status Update API + Analytics Integration
**Files:** `app/api/admin/club-orders/[orderId]/status/route.ts`, `app/api/admin/analytics/route.ts`, `app/api/admin/club-orders/route.ts`
**Action:**
- Create POST endpoint for mark_paid, mark_shipped, mark_fulfilled actions
- Add fulfillment counts to analytics API response
- Update club-orders list API to return all statuses (not just pending)
**Verify:** API route compiles, analytics includes fulfillmentCounts
**Done:** Status route handles all 3 actions, analytics returns pipeline counts

### Task 3: Enhanced Fulfillment Pipeline UI
**Files:** `app/admin/orders/PendingApprovalTab.tsx`, `app/admin/orders/OrdersClient.tsx`
**Action:**
- Add pipeline visualization bar with counts per stage
- Add status filter pills
- Add context-aware action buttons (Mark Paid, Mark Shipped with tracking, Mark Fulfilled)
- Add order timeline in expanded view showing timestamps
- Update OrdersClient to pass fulfillment counts
**Verify:** `npm run build` passes, UI renders pipeline bar and actions
**Done:** Full lifecycle visible and actionable in admin dashboard
