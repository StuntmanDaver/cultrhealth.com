---
phase: "01-bootstrap"
plan: "01-01"
type: "config"
wave: 1
depends_on: []
files_modified:
  - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/package.json
  - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/tsconfig.json
  - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/tailwind.config.ts
  - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/postcss.config.js
  - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/next.config.js
  - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/wrangler.toml
  - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/.env.example
  - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/.gitignore
  - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/app/globals.css
  - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/app/layout.tsx
autonomous: true
must_haves:
  truths:
    - cultrclub-web directory exists at /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/
    - wrangler.toml has compatibility_flags = ["nodejs_compat"]
    - next.config.js has images.unoptimized = true
    - package.json has build:cf script using @cloudflare/next-on-pages
    - tailwind.config.ts includes all brand color tokens from cultrhealth-website
  artifacts:
    - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/package.json
    - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/wrangler.toml
    - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/next.config.js
    - /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/tailwind.config.ts
---

# Plan 01-01: Bootstrap cultrclub-web Repo

## Objective

Create the `cultrclub-web` repository skeleton at `/Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/` with all top-level config files needed to build and deploy to Cloudflare Pages. No source files yet — just the scaffold.

## Context

- **Source plan:** `/Users/davidk/.claude/plans/snazzy-humming-treasure.md` (Phase 1 + Phase 4)
- **Target directory:** `/Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/`
- **Reference:** Brand tokens in `/Users/davidk/Documents/Dev-Projects/App-Ideas/Cultr Health Website/tailwind.config.ts`
- **Reference:** Fonts/animations in `/Users/davidk/Documents/Dev-Projects/App-Ideas/Cultr Health Website/app/globals.css`

## Tasks

### Task 1: Initialize repo directory + git

```bash
mkdir -p /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web
cd /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web
git init
```

### Task 2: Write package.json

```json
{
  "name": "cultrclub-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:cf": "npx @cloudflare/next-on-pages@1",
    "start": "next start",
    "preview": "npx wrangler pages dev .vercel/output/static",
    "deploy:staging": "npx wrangler pages deploy .vercel/output/static --branch=staging",
    "deploy:prod": "npx wrangler pages deploy .vercel/output/static --branch=main"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "@neondatabase/serverless": "^0.9.0",
    "jose": "^6.1.3",
    "resend": "^4.0.0",
    "lucide-react": "^0.563.0",
    "framer-motion": "^11.0.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.4.0",
    "@marsidev/react-turnstile": "^1.0.2"
  },
  "devDependencies": {
    "@cloudflare/next-on-pages": "^1.13.0",
    "typescript": "^5.4.0",
    "tailwindcss": "^3.4.3",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "@tailwindcss/typography": "^0.5.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@types/node": "^20.0.0"
  }
}
```

### Task 3: Write tsconfig.json

Standard Next.js 14 config with path alias `@/*` → root and `node` module resolution (matches cultrhealth-website):

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": false,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

### Task 4: Write wrangler.toml

```toml
name = "cultrclub"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"
```

### Task 5: Write next.config.js

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'Content-Security-Policy',
            value: [
              "default-src 'self'",
              "script-src 'self' 'unsafe-inline' https://challenges.cloudflare.com",
              "style-src 'self' 'unsafe-inline' https://fonts.googleapis.com",
              "font-src 'self' https://fonts.gstatic.com",
              "img-src 'self' data: blob:",
              "connect-src 'self' https://challenges.cloudflare.com",
              "frame-src 'self' https://challenges.cloudflare.com",
            ].join('; '),
          },
          { key: 'X-Frame-Options', value: 'SAMEORIGIN' },
          { key: 'X-Content-Type-Options', value: 'nosniff' },
          { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
        ],
      },
    ]
  },
}
module.exports = nextConfig
```

### Task 6: Write postcss.config.js

```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

### Task 7: Write tailwind.config.ts

Copy brand color tokens from `/Users/davidk/Documents/Dev-Projects/App-Ideas/Cultr Health Website/tailwind.config.ts`. Must include all `brand-*`, `forest`, `cream`, `sage`, `mint`, `aura-*`, `cultr-*` tokens and the same fontFamily config (fraunces, display, body). Include all custom keyframes (fadeIn, slideUp, float, shimmer, scaleIn, blurIn, bounceSubtle, glowPulse).

Read the source tailwind.config.ts, then write the adapted version for cultrclub-web.

### Task 8: Write app/globals.css

Copy from `/Users/davidk/Documents/Dev-Projects/App-Ideas/Cultr Health Website/app/globals.css`. Include Tailwind directives, CSS variables for fonts, brand colors, and all animation classes.

### Task 9: Write minimal app/layout.tsx

```typescript
import type { Metadata, Viewport } from 'next'
import { Fraunces, Playfair_Display, Inter } from 'next/font/google'
import './globals.css'

const fraunces = Fraunces({
  subsets: ['latin'],
  variable: '--font-fraunces',
  display: 'swap',
})

const playfair = Playfair_Display({
  subsets: ['latin'],
  variable: '--font-display',
  display: 'swap',
})

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-body',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'CULTR Club',
  description: 'Join the CULTR Club — wellness education, community, and exclusive member benefits.',
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${fraunces.variable} ${playfair.variable} ${inter.variable}`}>
      <body className="bg-brand-cream font-body">{children}</body>
    </html>
  )
}
```

### Task 10: Write .env.example

```bash
# Database
POSTGRES_URL=

# Auth
JWT_SECRET=
SESSION_SECRET=

# Email
RESEND_API_KEY=
FROM_EMAIL=
ADMIN_APPROVAL_EMAIL=

# Admin URL (for approval email links — points to cultrhealth.com admin)
ADMIN_BASE_URL=https://cultrhealth.com

# Site URL (cultrclub.com or staging URL)
NEXT_PUBLIC_SITE_URL=https://cultrclub.com

# Bot protection
TURNSTILE_SECRET_KEY=
NEXT_PUBLIC_TURNSTILE_SITE_KEY=

# Mailchimp
MAILCHIMP_API_KEY=
MAILCHIMP_AUDIENCE_ID=
MAILCHIMP_SERVER_PREFIX=

# Rate limiting
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
```

### Task 11: Write .gitignore

Copy standard Next.js .gitignore. Include: `.env*.local`, `.vercel`, `node_modules`, `.next`, `out`.

### Task 12: Initial git commit

```bash
cd /Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web
git add -A
git commit -m "chore: initialize cultrclub-web repo scaffold"
```

## Verification

- [ ] `/Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/` exists
- [ ] `wrangler.toml` contains `nodejs_compat` in `compatibility_flags`
- [ ] `next.config.js` has `images: { unoptimized: true }`
- [ ] `package.json` has `"build:cf": "npx @cloudflare/next-on-pages@1"`
- [ ] `tailwind.config.ts` has `brand-primary`, `forest`, `cream`, `sage`, `mint` color tokens
- [ ] `app/layout.tsx` has Fraunces, Playfair Display, Inter fonts loaded
- [ ] Git repo initialized with initial commit
