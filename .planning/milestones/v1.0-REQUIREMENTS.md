# Requirements Archive: v1.0 cultrclub-web Cloudflare Migration

**Archived:** 2026-04-27
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: cultrclub-web Cloudflare Migration

**Defined:** 2026-04-13
**Shipped:** 2026-04-22
**Core Value:** CULTR Club experience at cultrclub.com — standalone Cloudflare Pages app sharing Neon DB with cultrhealth.com admin.

## v1 Requirements

### Repo & Config

- [x] **CF-01**: cultrclub-web repo exists at `/Users/davidk/Documents/Dev-Projects/App-Ideas/cultrclub-web/` with Next.js 14 App Router
- [x] **CF-02**: `wrangler.toml` configured with `nodejs_compat` flag and correct build output dir
- [x] **CF-03**: `next.config.js` has `images.unoptimized: true` and Turnstile-only CSP headers
- [x] **CF-04**: `package.json` has `build:cf`, `preview`, `deploy:staging`, `deploy:prod` scripts using `@cloudflare/next-on-pages`
- [x] **CF-05**: `@neondatabase/serverless` used for all DB access; `neon()` instantiated with `fullResults: true`

### Source Extraction

- [x] **EX-01**: All 6 app routes and 1 new health route exist in cultrclub-web
- [x] **EX-02**: All 15 lib files copied and adapted (sql import swapped to `@/lib/db`)
- [x] **EX-03**: All 3 component files and hooks copied
- [x] **EX-04**: All product images (24 PNGs), logos, and email images copied to public/

### Code Adaptation

- [x] **CD-01**: `lib/db.ts` completely rewritten to use `@neondatabase/serverless` with `fullResults: true`
- [x] **CD-02**: All simple sql files have import swapped from `@vercel/postgres` to `@/lib/db`
- [x] **CD-03**: Transaction files (`app/api/club/orders/route.ts`, `lib/creators/commission.ts`) use `createPool()` + `Pool.connect()` instead of `db.connect()`
- [x] **CD-04**: `ADMIN_BASE_URL` env var used for approval email links (points to cultrhealth.com); `siteUrl` uses `NEXT_PUBLIC_SITE_URL`
- [x] **CD-05**: Welcome email in `app/api/club/signup/route.ts` links to `cultrclub.com` (not join.cultrhealth.com)
- [x] **CD-06**: `JoinLandingClient.tsx` cookie clear uses `getCookieDomain()` — no hardcoded `.cultrhealth.com`
- [x] **CD-07**: `lib/utils.ts` getCookieDomain() returns `.cultrclub.com` for cultrclub.com URLs
- [x] **CD-08**: All 6 API routes have `export const runtime = 'edge'` at top
- [x] **CD-09**: `app/layout.tsx` is minimal — no PaymentProviderLoader, includes brand fonts
- [x] **CD-10**: `middleware.ts` simplified — UTM tracking only, no join host detection, no HIPAA session timeout

### Deployment

- [x] **DP-01**: Cloudflare Pages project created, connected to `cultrclub-web` repo
- [x] **DP-02**: All 17 env vars set in Cloudflare Pages dashboard (production)
- [x] **DP-03**: `staging.join.cultrhealth.com` CNAME points to Cloudflare Pages staging deployment
- [x] **DP-04**: All 11 staging validation checks pass before production cutover

### Production Cutover

- [x] **CU-01**: `cultrclub.com` domain configured in Cloudflare Pages (production branch = `main`)
- [x] **CU-02**: `join.cultrhealth.com` retired — DNS removed, Vercel alias detached via API (NXDOMAIN, not 301; deviation from original plan documented in 05-02-SUMMARY.md)
- [x] **CU-03**: cultrhealth.com `middleware.ts` cleaned up (join host detection blocks removed)
- [x] **CU-04**: `join.cultrhealth.com` Vercel domain alias removed via API (Apr 22 2026 — completed same-day rather than 2-4 weeks after redirect, since traffic had already migrated)

## Outcomes

All 22 v1 requirements completed Apr 22 2026. Two minor deviations:
- **CU-02**: NXDOMAIN instead of 301 redirect (cleaner, no stale traffic to maintain).
- **CU-04**: Removed same-day instead of after 2-4 week monitoring (traffic data showed no return visitors). Vercel API removal lesson: dashboard removal didn't stick — required API call.
