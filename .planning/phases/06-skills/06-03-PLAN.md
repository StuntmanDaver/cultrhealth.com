---
phase: 06-skills
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/siphox-api/SKILL.md
  - .gitignore
autonomous: true
requirements: [SKL-03, SKL-04, SKL-05]
tags: [skills, documentation, siphox, gitignore, deferred]

must_haves:
  truths:
    - "The new gotcha at the TOP of the SiPhox skill's Gotchas section calls out the live `Authorization: Bearer` bug at lib/siphox/client.ts:80 and notes it will be fixed in Phase 13"
    - "The siphox-api skill remains otherwise unchanged — no other rewrites, no structural reorgs (D-08 specifies a targeted edit)"
    - ".gitignore allows .claude/skills/corepay-api/ and .claude/skills/healthie-api/ to be committed (matching the existing siphox-api exception pattern)"
    - "No corpay-crossborder skill directory or .gitignore exception is created (D-12 explicitly drops SKL-03)"
    - "The Phase 6 summary records SKL-03 as deferred-not-shipped per D-12 so audit trails reflect the scope decision"
  artifacts:
    - path: ".claude/skills/siphox-api/SKILL.md"
      provides: "Refreshed SiPhox skill with new repo-bug gotcha at top of Gotchas list"
      contains: "Authorization Bearer bug; lib/siphox/client.ts:80; Phase 13 fix marker"
      min_lines: 100
    - path: ".gitignore"
      provides: "Project-specific skill directories committed (corepay-api + healthie-api)"
      contains: "negation lines for corepay-api and healthie-api skill directories"
      min_lines: 50
  key_links:
    - from: ".claude/skills/siphox-api/SKILL.md Gotchas section"
      to: "lib/siphox/client.ts:80 bug pointer"
      via: "first numbered gotcha"
      pattern: "lib/siphox/client\\.ts:80"
    - from: ".gitignore project-specific skills block"
      to: "corepay-api and healthie-api directories"
      via: "negation patterns matching siphox-api precedent"
      pattern: "!\\.claude/skills/(corepay-api|healthie-api)/"
---

<objective>
Three small, independent edits in one plan:

1. SKL-04 — Add the "Known repo bug" gotcha to .claude/skills/siphox-api/SKILL.md (D-08 — targeted edit only; insert as the new #1 gotcha, renumber existing).
2. SKL-05 — Append two .gitignore exceptions (corepay-api, healthie-api) under the existing project-specific-skills block (D-11). Do NOT add a corpay-crossborder exception (D-12 drops SKL-03).
3. SKL-03 (deferred) — Document that SKL-03 is intentionally not shipped per D-12 in this plan's summary so audit trails reflect the scope decision. NO corpay-crossborder skill file is created.

Purpose: Close out the small but critical Phase 6 cleanup tasks that don't fit naturally into the Corepay or Healthie skill plans. The .gitignore changes unblock Plans 01 and 02's outputs from being .gitignored at commit time.

Output: Two file edits + one summary record. No new directories or files (apart from the summary).

Implements: SKL-03 (deferred per D-12), SKL-04 (siphox edit per D-08), SKL-05 (.gitignore per D-11).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skills/06-CONTEXT.md
@.planning/phases/06-skills/06-PATTERNS.md
@.claude/skills/siphox-api/SKILL.md
@lib/siphox/client.ts
@.gitignore

<interfaces>
Source-of-truth excerpts for the targeted edits.

Current siphox-api SKILL.md Gotchas section (lines 19-30):
- Line 19: `## Gotchas — read these first`
- Line 20: blank
- Line 21: `1. **Auth prefix is ` + backtick + `Token` + backtick + `, not ` + backtick + `Bearer` + backtick + `.** ...`
- Lines 22-30: gotchas 2-10

After D-08 edit, gotcha #1 becomes the repo-bug callout, and existing 1-10 renumber to 2-11.

Bug location confirmed in lib/siphox/client.ts (lines 78-82):
```
const headers: Record<string, string> = {
  'Authorization': `Bearer ${apiKey}`,   // line 80 — should be Token, not Bearer
  'Content-Type': 'application/json',
}
```

Current .gitignore (lines 47-51):
```
.claude/skills/*
# Project-specific skills (shared with team)
!.claude/skills/siphox-api/
skills-lock.json
```

After D-11 edit (two new lines inserted between siphox-api line and skills-lock.json line; NO corpay-crossborder line):
```
.claude/skills/*
# Project-specific skills (shared with team)
!.claude/skills/siphox-api/
!.claude/skills/corepay-api/
!.claude/skills/healthie-api/
skills-lock.json
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert repo-bug gotcha into .claude/skills/siphox-api/SKILL.md</name>
  <files>.claude/skills/siphox-api/SKILL.md</files>
  <read_first>
    - .claude/skills/siphox-api/SKILL.md (current state — read full file before editing to see exact line numbers and existing gotcha numbering)
    - lib/siphox/client.ts (specifically lines 78-82 to confirm the Authorization Bearer bug is still at line 80)
    - .planning/phases/06-skills/06-CONTEXT.md (D-08 — targeted edit, no other changes)
    - .planning/phases/06-skills/06-PATTERNS.md (siphox-api refresh section — exact insertion pattern)
  </read_first>

  <action>
Step 1 — Verify bug location. Run `grep -n "Bearer" lib/siphox/client.ts | head -5` to confirm the literal `Bearer` token appears at or near line 80. D-08 specifically calls out line 80; verify and report any drift in the plan summary.

Step 2 — Edit .claude/skills/siphox-api/SKILL.md using the Edit tool. The change is targeted: insert ONE new gotcha at the top of the Gotchas list, then renumber existing gotchas 1-10 to 2-11.

Find this exact block (current siphox-api SKILL.md lines 19-30 — read the file first to confirm exact text):

```
## Gotchas — read these first

1. **Auth prefix is `Token`, not `Bearer`.** `Authorization: Token <uuid>`. Anything else returns 401 "Business session is required".
2. **No `servers` block in the spec.** Always prepend `https://connect.siphoxhealth.com` manually — the spec ships with `servers: []`.
3. **Kit registration is async.** `POST /api/v1/kits/{kitID}/register` returns 200 immediately; lab processing happens later. Poll `/api/v1/kits` or `/api/v1/customers/{id}` to detect `sampleStatus = resulted`.
4. **Test orders require `is_test_order: true`** on `/create-order`. Otherwise you burn real credits / charge the attached payment method.
5. **`purchase_with_attached_payment` wording is ambiguous.** Per the spec, `false` first tries credits, then the attached payment method, then falls back to creating a pending order. Re-check the admin dashboard before sending anything else.
6. **`sai/upsells` uses stringified JSON.** `productOfferingsJson` is a string containing JSON — `JSON.stringify` the array on write, `JSON.parse` on read.
7. **503 "NOT_AVAILABLE"** is a legitimate response (e.g. `/kits/{kitID}/register` when the feature is not enabled for a business). Don't retry — surface to admin.
8. **Three separate status fields** on kits/samples/orders. Don't conflate them. See `references/kit-lifecycle.md`.
9. **Only four named schemas** — `Address`, `CreateOrderRequest`, `ProductOffering`, `Suggestion`. Everything else is inline. See `references/schemas.md`.
10. **GitBook docs are thin.** The OpenAPI spec is authoritative. If the spec and GitBook disagree, trust the spec and verify with a test order.
```

Replace with this exact block (one new #1 inserted at top, all existing gotchas renumbered 2-11):

```
## Gotchas — read these first

1. **Known repo bug: `lib/siphox/client.ts:80` sends `Authorization: Bearer` but SiPhox requires `Authorization: Token`.** The existing code is broken for auth and returns 401 "Business session is required" in production. Will be fixed in Phase 13. Until then, do not rely on `lib/siphox/client.ts` for live API calls — use the `siphoxFetch` helper in `references/client-starter.ts` instead.
2. **Auth prefix is `Token`, not `Bearer`.** `Authorization: Token <uuid>`. Anything else returns 401 "Business session is required".
3. **No `servers` block in the spec.** Always prepend `https://connect.siphoxhealth.com` manually — the spec ships with `servers: []`.
4. **Kit registration is async.** `POST /api/v1/kits/{kitID}/register` returns 200 immediately; lab processing happens later. Poll `/api/v1/kits` or `/api/v1/customers/{id}` to detect `sampleStatus = resulted`.
5. **Test orders require `is_test_order: true`** on `/create-order`. Otherwise you burn real credits / charge the attached payment method.
6. **`purchase_with_attached_payment` wording is ambiguous.** Per the spec, `false` first tries credits, then the attached payment method, then falls back to creating a pending order. Re-check the admin dashboard before sending anything else.
7. **`sai/upsells` uses stringified JSON.** `productOfferingsJson` is a string containing JSON — `JSON.stringify` the array on write, `JSON.parse` on read.
8. **503 "NOT_AVAILABLE"** is a legitimate response (e.g. `/kits/{kitID}/register` when the feature is not enabled for a business). Don't retry — surface to admin.
9. **Three separate status fields** on kits/samples/orders. Don't conflate them. See `references/kit-lifecycle.md`.
10. **Only four named schemas** — `Address`, `CreateOrderRequest`, `ProductOffering`, `Suggestion`. Everything else is inline. See `references/schemas.md`.
11. **GitBook docs are thin.** The OpenAPI spec is authoritative. If the spec and GitBook disagree, trust the spec and verify with a test order.
```

Constraints (D-08):
- Do NOT touch any other section of the SiPhox skill (no rewrites of frontmatter, Overview, Quick reference table, etc.)
- Do NOT change the "## Gotchas — read these first" heading text
- Total gotchas after edit: exactly 11 (was 10, added 1)
- The bug-pointer line MUST contain the literal string `lib/siphox/client.ts:80` (D-08 explicit requirement) and the literal string `Phase 13`

If grep shows the bug is no longer at line 80 (e.g., the file was edited recently), use the actual current line number in the gotcha text but flag the line drift in the plan summary.
  </action>

  <acceptance_criteria>
    - File still exists: test -f .claude/skills/siphox-api/SKILL.md returns success
    - Gotcha #1 is the repo-bug callout: `grep -E "^1\. \*\*Known repo bug" .claude/skills/siphox-api/SKILL.md` returns >= 1 match
    - Bug location reference: `grep "lib/siphox/client.ts:80" .claude/skills/siphox-api/SKILL.md` returns >= 1 match
    - Phase 13 fix marker: `grep "Phase 13" .claude/skills/siphox-api/SKILL.md` returns >= 1 match
    - Bearer/Token contrast: the gotcha text contains both `Authorization: Bearer` and `Authorization: Token`. Verify with `grep -c "Authorization: Bearer" .claude/skills/siphox-api/SKILL.md` >= 1 AND `grep -c "Authorization: Token" .claude/skills/siphox-api/SKILL.md` >= 1
    - Gotcha #2 is the original #1 (auth-prefix rule): `grep -E "^2\. \*\*Auth prefix is " .claude/skills/siphox-api/SKILL.md` returns >= 1 match
    - Total numbered gotchas count is 11: `grep -cE "^[0-9]+\. \*\*" .claude/skills/siphox-api/SKILL.md` returns 11
    - Last gotcha is #11 (renumbering complete): `grep -E "^11\. \*\*GitBook docs" .claude/skills/siphox-api/SKILL.md` returns 1 match
    - No gotcha #12: `grep -E "^12\. \*\*" .claude/skills/siphox-api/SKILL.md` returns 0 matches
    - Frontmatter unchanged: `head -4 .claude/skills/siphox-api/SKILL.md | grep -c "^---$"` returns 2; `grep "^name: siphox-api" .claude/skills/siphox-api/SKILL.md` returns 1
    - Other top-level sections still present: `grep -cE "^## (Overview|Quick reference|When implementing|Cultr integration|Reference files|Refreshing this skill)" .claude/skills/siphox-api/SKILL.md` returns >= 5
  </acceptance_criteria>

  <verify>
    <automated>grep -q "lib/siphox/client.ts:80" .claude/skills/siphox-api/SKILL.md && grep -q "Phase 13" .claude/skills/siphox-api/SKILL.md && [ "$(grep -cE '^[0-9]+\. \*\*' .claude/skills/siphox-api/SKILL.md)" = "11" ] && grep -qE "^1\. \*\*Known repo bug" .claude/skills/siphox-api/SKILL.md && grep -qE "^11\. \*\*GitBook docs" .claude/skills/siphox-api/SKILL.md && echo PASS</automated>
  </verify>

  <done>
    SiPhox skill has new gotcha #1 pointing at the live repo bug; existing gotchas renumbered to 2-11; rest of file untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add .gitignore exceptions for corepay-api and healthie-api</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current state — read full file to see existing exception block at lines 47-51)
    - .planning/phases/06-skills/06-CONTEXT.md (D-11 — exact two lines to add; D-12 — NO corpay-crossborder line)
    - .planning/phases/06-skills/06-PATTERNS.md (.gitignore section — exact insertion location and lines)
  </read_first>

  <action>
Step 1 — Verify current state. Run `grep -n "skills" .gitignore | head -10` to confirm:
- A line containing `.claude/skills/*`
- A line containing `# Project-specific skills (shared with team)`
- A line containing `!.claude/skills/siphox-api/`
- A line containing `skills-lock.json`

If line numbers have drifted, identify the actual lines by content matching, not line number.

Step 2 — Edit .gitignore using the Edit tool. Insert TWO new lines between the siphox-api negation and `skills-lock.json`.

Find this exact block:
```
.claude/skills/*
# Project-specific skills (shared with team)
!.claude/skills/siphox-api/
skills-lock.json
```

Replace with this exact block:
```
.claude/skills/*
# Project-specific skills (shared with team)
!.claude/skills/siphox-api/
!.claude/skills/corepay-api/
!.claude/skills/healthie-api/
skills-lock.json
```

Constraints (D-11, D-12):
- ONLY two new lines added: `!.claude/skills/corepay-api/` and `!.claude/skills/healthie-api/`
- NO `!.claude/skills/corpay-crossborder/` line — SKL-03 is dropped per D-12
- The comment line stays unchanged
- The siphox-api line stays unchanged
- The skills-lock.json line stays unchanged in its same relative position (after the new exceptions)
- Trailing newline preserved (no blank-line drift at file end)

After the edit, the file should have exactly 3 negation lines for skills (siphox-api, corepay-api, healthie-api) — no more, no less.
  </action>

  <acceptance_criteria>
    - File still exists: test -f .gitignore returns success
    - corepay-api exception present: `grep -E "^!\.claude/skills/corepay-api/$" .gitignore` returns 1 match
    - healthie-api exception present: `grep -E "^!\.claude/skills/healthie-api/$" .gitignore` returns 1 match
    - siphox-api exception still present: `grep -E "^!\.claude/skills/siphox-api/$" .gitignore` returns 1 match
    - NO corpay-crossborder exception (D-12): `grep "corpay-crossborder" .gitignore` returns 0 matches
    - Total skill negation count is exactly 3: `grep -cE "^!\.claude/skills/" .gitignore` returns 3
    - Order preserved (siphox before new entries): `grep -nE "^!\.claude/skills/" .gitignore | head -1 | grep "siphox-api"` returns the siphox-api line first
    - Comment line still present: `grep "^# Project-specific skills" .gitignore` returns 1 match
    - skills-lock.json line still present: `grep "^skills-lock.json$" .gitignore` returns 1 match
    - Wildcard line still present: `grep -E "^\.claude/skills/\*$" .gitignore` returns 1 match
    - File length sanity: `wc -l .gitignore | awk '{print $1}'` returns >= 50
  </acceptance_criteria>

  <verify>
    <automated>grep -qE "^!\.claude/skills/corepay-api/$" .gitignore && grep -qE "^!\.claude/skills/healthie-api/$" .gitignore && grep -qE "^!\.claude/skills/siphox-api/$" .gitignore && [ "$(grep -cE '^!\.claude/skills/' .gitignore)" = "3" ] && ! grep -q "corpay-crossborder" .gitignore && echo PASS</automated>
  </verify>

  <done>
    .gitignore has 3 skill negation lines (siphox-api, corepay-api, healthie-api). No corpay-crossborder line. Comment block and skills-lock.json line preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record SKL-03 deferral in plan summary</name>
  <files>.planning/phases/06-skills/06-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/06-skills/06-CONTEXT.md (D-12 — explicit drop of SKL-03; rationale: Corpay/Fleetcor has zero presence in source code)
    - .planning/REQUIREMENTS.md lines 21-23 (SKL-03 original spec — must be marked as historical record, not removed)
  </read_first>

  <action>
This is the standard SUMMARY.md that every plan produces, but for Plan 03 it MUST also explicitly record the SKL-03 deferral so audit trails reflect the scope decision per D-12.

Create `.planning/phases/06-skills/06-03-SUMMARY.md` with these required sections:

1. Header with plan ID, completion date, status.
2. Section "Files modified": list .claude/skills/siphox-api/SKILL.md and .gitignore (and provide line-count diffs if available).
3. Section "Tasks completed": Task 1 (siphox edit), Task 2 (.gitignore), Task 3 (this summary).
4. Section "SKL-03 status: DEFERRED PER D-12" containing:
   - Original SKL-03 spec text quoted from REQUIREMENTS.md (the "corpay-crossborder/SKILL.md exists as reference-only..." sentence)
   - Decision reference: "D-12 in .planning/phases/06-skills/06-CONTEXT.md drops SKL-03 from Phase 6 scope. The corpay-crossborder skill was a disambiguation guard for a company (Corpay / Fleetcor) with zero presence in the source code; a guard skill adds noise without value. The disambiguation is captured in CLAUDE.md MEMORY index entry feedback_corepay_vs_corpay_vs_authorize_net.md AND in the corepay-api skill's Vocabulary section (Plan 01)."
   - Future trigger: "If creator international payouts are ever scoped, a Corpay research skill can be created at that point. Until then, SKL-03 is intentionally not shipped."
   - Coverage note: "REQUIREMENTS.md SKL-03 entry remains as a historical record. The line is not deleted; only the deliverable is deferred."
5. Section "Verification": confirm Task 1 and Task 2 acceptance criteria all passed (paste the verify-command outputs).
6. Section "Sanity check": confirm three skills are present and committable:
   - `[ -f .claude/skills/corepay-api/SKILL.md ]` — should be true after Plan 01 ships
   - `[ -f .claude/skills/healthie-api/SKILL.md ]` — should be true after Plan 02 ships
   - `[ ! -d .claude/skills/corpay-crossborder/ ]` — MUST be true (D-12 invariant)
   - `[ "$(grep -cE '^!\.claude/skills/' .gitignore)" = "3" ]` — must equal exactly 3
7. Section "Phase 6 progress": list which Phase 6 requirements are now satisfied/deferred:
   - SKL-01: satisfied by Plan 01 (corepay-api skill)
   - SKL-02: satisfied by Plan 02 (healthie-api skill)
   - SKL-03: DEFERRED per D-12 (no corpay-crossborder skill, by design)
   - SKL-04: satisfied by this plan (Task 1 — siphox refresh)
   - SKL-05: satisfied by this plan (Task 2 — .gitignore exceptions)

The summary file is the audit artifact for the SKL-03 scope reduction decision.
  </action>

  <acceptance_criteria>
    - File exists: test -f .planning/phases/06-skills/06-03-SUMMARY.md returns success
    - SKL-03 deferral section present: `grep -E "SKL-03.*DEFERRED|DEFERRED.*SKL-03|SKL-03.*deferred" .planning/phases/06-skills/06-03-SUMMARY.md` returns >= 1 match
    - D-12 reference: `grep "D-12" .planning/phases/06-skills/06-03-SUMMARY.md` returns >= 1 match
    - All five SKL- requirements named: `grep -cE "SKL-0[1-5]" .planning/phases/06-skills/06-03-SUMMARY.md` returns >= 5
    - corpay-crossborder explicitly mentioned and disambiguated: `grep "corpay-crossborder" .planning/phases/06-skills/06-03-SUMMARY.md` returns >= 1 match
    - Future-trigger note about creator international payouts: `grep -i "creator international payouts\|international payout" .planning/phases/06-skills/06-03-SUMMARY.md` returns >= 1 match
    - Sanity check section: `grep -E "Sanity check|Verification" .planning/phases/06-skills/06-03-SUMMARY.md` returns >= 1 match
    - File length sanity: `wc -l .planning/phases/06-skills/06-03-SUMMARY.md | awk '{print $1}'` returns >= 30
  </acceptance_criteria>

  <verify>
    <automated>test -f .planning/phases/06-skills/06-03-SUMMARY.md && grep -q "D-12" .planning/phases/06-skills/06-03-SUMMARY.md && grep -qE "SKL-03.*DEFERRED|SKL-03.*deferred" .planning/phases/06-skills/06-03-SUMMARY.md && grep -q "corpay-crossborder" .planning/phases/06-skills/06-03-SUMMARY.md && [ "$(grep -cE 'SKL-0[1-5]' .planning/phases/06-skills/06-03-SUMMARY.md)" -ge 5 ] && echo PASS</automated>
  </verify>

  <done>
    Plan 03 summary records both completed work (Tasks 1 and 2) AND the SKL-03 deferral with D-12 rationale. All five SKL requirements have an explicit status entry.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure documentation/config phase. No API endpoints. No auth changes. No PHI. No new dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | .gitignore edit could accidentally negate too broad a pattern | mitigate | Acceptance criteria checks exact negation count is 3 (siphox + corepay + healthie). No wildcard negation; explicit per-directory entries only. |
| T-06-09 | Information Disclosure | siphox-api skill edit could accidentally remove other gotchas | mitigate | Acceptance criteria checks total numbered gotchas count is exactly 11 (was 10, +1) AND that gotchas #2-#11 retain their original opening text (e.g., "Auth prefix is Token", "GitBook docs are thin"). |
| T-06-10 | Repudiation | SKL-03 scope reduction without record | mitigate | Task 3 produces a SUMMARY.md that explicitly records the deferral with D-12 rationale and future-trigger conditions; REQUIREMENTS.md entry stays as historical record. |

**Threat surface assessment:** Documentation/config-only deliverable. The .gitignore change is the only one that affects build/deploy behavior — it changes which files get committed, which is mitigated by the strict acceptance check that exactly 3 skill negations exist.
</threat_model>

<verification>
Run all three task verify commands. Confirm:
- siphox-api skill has 11 numbered gotchas, with #1 being the repo-bug callout
- .gitignore has exactly 3 skill negations (siphox + corepay + healthie), no corpay-crossborder
- SUMMARY.md records SKL-03 as deferred per D-12 with future-trigger conditions
- No corpay-crossborder skill directory was created
</verification>

<success_criteria>
- SKL-04 satisfied: SiPhox skill carries the live repo bug gotcha at top of Gotchas list
- SKL-05 satisfied: .gitignore lets corepay-api and healthie-api skill directories be committed
- SKL-03 satisfied (as deferred per D-12): documented in plan summary; no skill file created
</success_criteria>

<output>
After completion, the SUMMARY.md described in Task 3 IS the plan summary. Verify it covers all three tasks AND the SKL-03 deferral decision.
</output>
