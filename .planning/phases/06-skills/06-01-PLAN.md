---
phase: 06-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/corepay-api/SKILL.md
autonomous: true
requirements: [SKL-01]
tags: [skills, documentation, corepay, authorize-net, payments]

must_haves:
  truths:
    - "Claude Code loads the corepay-api skill when a session touches COREPAY_TRANSACTION_KEY, NEXT_PUBLIC_COREPAY_API_LOGIN_ID, gatewayFetch, ARBCreateSubscriptionRequest, createCustomerProfile, or any lib/payments/corepay-*.ts file"
    - "The Vocabulary section is the first content after the title and disambiguates Corepay (corepay.net merchant ISO) from Authorize.Net (gateway) from Corpay (corpay.com Fleetcor B2B FX), and explicitly names developer.crossborder.corpay.com as the trap URL"
    - "The skill documents the seven critical gotchas with failure modes: no authorizenet npm SDK (with HMAC failure mode), gatewayFetch() injects merchantAuthentication automatically, Accept.js single-use 15-min token, HMAC-SHA512 over request.text(), ARB updates apply next-cycle-only, sandbox/production credential mismatch (Code 13), webhook IP allowlist (198.241.206.38 / 198.241.207.38)"
    - "Sections describing API surface not yet implemented (CIM, Accept.js client flow, HMAC-SHA512 webhook route, ARB update next-cycle) are bracketed with HTML comment markers per D-01 so they are spec-forward without being mistaken for current code"
  artifacts:
    - path: ".claude/skills/corepay-api/SKILL.md"
      provides: "Corepay/Authorize.Net skill loadable by Claude Code"
      contains: "Vocabulary, Gotchas, Quick reference, When implementing, CULTR integration patterns"
      min_lines: 200
  key_links:
    - from: ".claude/skills/corepay-api/SKILL.md frontmatter description"
      to: "trigger keywords COREPAY_TRANSACTION_KEY, gatewayFetch, ARBCreateSubscriptionRequest, createCustomerProfile, lib/payments/corepay-*.ts"
      via: "embedded in description prose"
      pattern: "gatewayFetch.*ARBCreateSubscriptionRequest|ARBCreateSubscriptionRequest.*gatewayFetch"
    - from: ".claude/skills/corepay-api/SKILL.md Vocabulary section"
      to: "developer.crossborder.corpay.com URL trap"
      via: "explicit do-not-confuse text"
      pattern: "developer\\.crossborder\\.corpay\\.com"
    - from: ".claude/skills/corepay-api/SKILL.md authorizenet gotcha"
      to: "HMAC webhook signature failure mode explanation"
      via: "Gotchas section #1"
      pattern: "authorizenet.*HMAC|HMAC.*authorizenet"
---

<objective>
Create `.claude/skills/corepay-api/SKILL.md` — a single-file Claude Code skill that future sessions auto-load when working on Corepay/Authorize.Net code. The skill is **spec-forward** per D-01: it documents the full intended API surface (CIM, Accept.js token flow, HMAC-SHA512 webhooks, ARB update next-cycle rule) needed for Phase 7, even though only ARB subscription create exists today. Unimplemented sections are bracketed with HTML comment markers so they are visible in the source but clearly marked as Phase 7 work.

Purpose: Stop the Corepay/Corpay/Authorize.Net naming confusion at the source. Stop new code from `npm install authorizenet`-ing (which breaks HMAC webhook verification). Give Phase 7 implementers the complete spec they need without re-researching from scratch.

Output: One markdown file at `.claude/skills/corepay-api/SKILL.md`, ~250 lines, mirroring the proven `siphox-api/SKILL.md` structure.

Implements: SKL-01 (per D-01, D-02, D-03, D-04, D-09, D-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skills/06-CONTEXT.md
@.planning/phases/06-skills/06-PATTERNS.md
@.claude/skills/siphox-api/SKILL.md
@lib/payments/corepay-gateway.ts
@lib/config/payments.ts

<interfaces>
<!-- Source-of-truth excerpts the skill must accurately describe. Pulled from the codebase so the executor does not re-explore. -->

From lib/payments/corepay-gateway.ts (lines 50-89) — the gatewayFetch pattern:
```typescript
async function gatewayFetch<T>(
  requestBody: Record<string, unknown>,
  overrides?: GatewayCredentials,
): Promise<T> {
  const url = overrides?.apiUrl || COREPAY_CONFIG.apiUrl;
  const auth = overrides
    ? { name: overrides.apiLoginId, transactionKey: overrides.transactionKey }
    : {
        name: process.env.NEXT_PUBLIC_COREPAY_API_LOGIN_ID || '',
        transactionKey: process.env.COREPAY_TRANSACTION_KEY || '',
      };
  const requestKey = Object.keys(requestBody)[0];
  const envelope = {
    [requestKey]: {
      ...requestBody[requestKey] as Record<string, unknown>,
      merchantAuthentication: auth,  // injected automatically
    },
  };
  // POST envelope to url; throw on resultCode === 'Error'
}
```

From lib/payments/corepay-gateway.ts (lines 95-147) — ARB usage:
```typescript
// gatewayFetch<SubscriptionResponse>(
//   { ARBCreateSubscriptionRequest: { subscription } },
//   params.credentials,  // optional override creds
// )
// Pass the Authorize.Net operation name as the top-level key.
// merchantAuthentication is injected automatically by gatewayFetch.
```

From lib/config/payments.ts (lines 34-45) — COREPAY_CONFIG:
```typescript
export const COREPAY_CONFIG = {
  apiUrl: process.env.COREPAY_ENVIRONMENT === 'production'
    ? 'https://api.authorize.net/xml/v1/request.api'
    : 'https://apitest.authorize.net/xml/v1/request.api',
  acceptJsUrl: process.env.COREPAY_ENVIRONMENT === 'production'
    ? 'https://js.authorize.net/v1/Accept.js'
    : 'https://jstest.authorize.net/v1/Accept.js',
  apiLoginId: process.env.NEXT_PUBLIC_COREPAY_API_LOGIN_ID || '',
  publicClientKey: process.env.NEXT_PUBLIC_COREPAY_PUBLIC_CLIENT_KEY || '',
  environment: (process.env.COREPAY_ENVIRONMENT || 'sandbox') as 'sandbox' | 'production',
  currency: 'USD',
} as const;
```

Env vars to document:
- NEXT_PUBLIC_COREPAY_API_LOGIN_ID — public, API login ID
- COREPAY_TRANSACTION_KEY — server-only, transaction signing key
- NEXT_PUBLIC_COREPAY_PUBLIC_CLIENT_KEY — public, Accept.js client key
- COREPAY_ENVIRONMENT — sandbox | production
- NEXT_PUBLIC_ENABLE_COREPAY — feature flag (`'true'` to enable)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .claude/skills/corepay-api/SKILL.md</name>
  <files>.claude/skills/corepay-api/SKILL.md</files>
  <read_first>
    - .claude/skills/siphox-api/SKILL.md (canonical format reference — copy structure exactly: frontmatter shape, "Gotchas — read these first" heading phrasing, Quick reference table format, "When implementing" section style)
    - lib/payments/corepay-gateway.ts (current ARB implementation; gatewayFetch helper at lines 50-89; createSubscription at lines 95-147 — these are the only Corepay code paths that exist today)
    - lib/config/payments.ts (COREPAY_CONFIG, env vars, sandbox/production toggle, COREPAY_ENABLED feature flag)
    - .planning/phases/06-skills/06-CONTEXT.md (D-01 through D-04 — spec-forward decisions)
    - .planning/phases/06-skills/06-PATTERNS.md (full pattern map for corepay-api skill — vocabulary section template, ARB call pattern, env vars list, spec-forward marker pattern)
    - CLAUDE.md (HIPAA logging rules — the skill must reinforce HIPAA discipline)
  </read_first>

  <action>
Create the file `.claude/skills/corepay-api/SKILL.md`. The directory `.claude/skills/corepay-api/` does not exist yet — the Write tool will create it.

**Frontmatter (exact text, lines 1-4):**

```
---
name: corepay-api
description: Use when integrating or calling the Corepay/Authorize.Net payment gateway — Accept.js token flow, ARB subscriptions, CIM customer profiles, webhook HMAC-SHA512 verification, or any code that touches `COREPAY_TRANSACTION_KEY`, `NEXT_PUBLIC_COREPAY_API_LOGIN_ID`, `gatewayFetch`, `ARBCreateSubscriptionRequest`, `createCustomerProfile`, or any `lib/payments/corepay-*.ts` file. NOTE: Corepay (corepay.net) is CULTR's payment ISO and uses Authorize.Net as the underlying gateway. This is NOT Corpay (corpay.com / Fleetcor), a B2B cross-border FX company with zero presence in this codebase.
---
```

(D-04 frontmatter trigger. The trigger keywords MUST appear in description prose verbatim.)

**Body sections in this exact order (D-03 places Vocabulary FIRST, before Overview):**

1. **Title:** `# Corepay / Authorize.Net Payment Gateway`

2. **Vocabulary section (D-03 — FIRST CONTENT, before Overview):**
   - Heading: `## Vocabulary — do not conflate these three companies`
   - Three-row table:

     | Name | Domain | Role | In CULTR codebase? |
     |---|---|---|---|
     | **Corepay** | corepay.net | CULTR's payment ISO / merchant acquirer | Yes — env vars, credentials |
     | **Authorize.Net** | authorize.net / apitest.authorize.net | Underlying payment gateway API | Yes — all API URLs |
     | **Corpay** | corpay.com / developer.crossborder.corpay.com | Fleetcor B2B cross-border FX / payouts | NO — zero presence |

   - Followed by a quoted callout containing the URL trap: `> The URL **developer.crossborder.corpay.com** is for Corpay (Fleetcor). Do not confuse it with CULTR's gateway. CULTR sends requests to api.authorize.net (production) or apitest.authorize.net (sandbox) using Corepay merchant credentials.`

3. **Overview section** — heading `## Overview`. Document:
   - Corepay = CULTR's merchant relationship; Authorize.Net = the gateway API surface
   - Production URL: `https://api.authorize.net/xml/v1/request.api`
   - Sandbox URL: `https://apitest.authorize.net/xml/v1/request.api`
   - Accept.js production: `https://js.authorize.net/v1/Accept.js`
   - Accept.js sandbox: `https://jstest.authorize.net/v1/Accept.js`
   - Auth: `merchantAuthentication: { name, transactionKey }` envelope on EVERY request body (not a header)
   - Env vars (with bullet list of the 5 listed in interfaces above and what each is for; mark `NEXT_PUBLIC_*` as client-readable, plain ones as server-only)
   - Current state: `NEXT_PUBLIC_ENABLE_COREPAY=false` everywhere as of 2026-04-28; only `lib/payments/corepay-gateway.ts:createSubscription` is implemented; full surface lands in Phase 7

4. **Gotchas — read these first (D-02, D-03 — exact heading: `## Gotchas — read these first`).** Numbered list, in this order:

   1. **Do NOT install the `authorizenet` npm package.** The SDK has known issues with HMAC signature envelope formatting that break webhook signature verification — it wraps the signature string incorrectly, causing every webhook to fail signature check. The repo uses native `fetch()` via `gatewayFetch()` in `lib/payments/corepay-gateway.ts` for exactly this reason. Phase 7 helpers (`authorize-net-cim.ts`, `authorize-net-arb.ts`, `authorize-net-charges.ts`) MUST extend `gatewayFetch()`, not `npm install authorizenet`. (Source: `.planning/research/SUMMARY.md`; D-02.)

   2. **`gatewayFetch()` injects `merchantAuthentication` automatically.** Pass the request body keyed by the Authorize.Net operation name (e.g. `{ ARBCreateSubscriptionRequest: { subscription } }`); the helper wraps it with `merchantAuthentication: { name, transactionKey }` before sending. Never include `merchantAuthentication` in the input object — you'll double-wrap and the request will fail.

   3. **Accept.js opaque token is single-use and expires in 15 minutes.** When a member tokenizes a card client-side, the resulting `opaqueData` (`{ dataDescriptor, dataValue }`) can be used in EXACTLY ONE charge or CIM `createCustomerProfile` call, then it's burned. If the user lingers on the confirm step past 15 minutes, the next charge attempt returns a token-expired error — handle by re-tokenizing, not by retrying.

   4. **Webhook signature is HMAC-SHA512 computed over the raw request body via `request.text()`.** Header is `X-ANET-Signature: sha512=<hex>`. Use length-checked `timingSafeEqual` (matched buffer lengths required, else `TypeError`). Read the body via `request.text()` BEFORE any other body access — Next.js consumes the body on first read and `request.json()` will mutate parsing in ways that break the HMAC source. (Spec-forward — Phase 7 will land `app/api/webhook/corepay/route.ts`.)

   5. **ARB subscription updates apply NEXT CYCLE ONLY.** `ARBUpdateSubscriptionRequest` does not retro-charge; it changes the amount/payment profile that bills on the next scheduled date. To apply an immediate change (e.g., a discount or new card), pair the ARB update with a separate one-shot `chargeCustomerProfile` (or `createTransactionRequest`) for the difference. The Q1 sandbox test confirmed: a card update via Path A (in-place CIM payment profile edit) does NOT cascade to the active ARB; you must call `ARBUpdateSubscriptionRequest` to point the subscription at the new payment profile (Path B is the documented default).

   6. **Sandbox credentials in production (or vice versa) silently fail with Authorize.Net Code 13 ("Merchant authentication failed").** Boot-time validation in `instrumentation.ts` (Phase 7, PLB-11) MUST call `authenticateTestRequest` and refuse to start the app on Code 13. Without this gate, the only signal is webhook signature mismatches every request — extremely hard to diagnose retroactively.

   7. **Authorize.Net webhook IPs to allowlist: `198.241.206.38` and `198.241.207.38`.** Phase 7 PLB-10 gates the route on these IPs as belt-and-braces with the HMAC signature. Authorize.Net occasionally rotates IPs; non-allowlist IP with a valid signature should be LOGGED, not rejected — graceful handling avoids outages on rotation day.

5. **Quick reference — operations.** Heading: `## Quick reference — operations`. Three-column table with these rows (mark unimplemented rows with `<!-- Phase 7 -->` HTML comment in the leftmost cell so they're flagged):

   | Operation | Module | Purpose |
   |---|---|---|
   | `ARBCreateSubscriptionRequest` | `lib/payments/corepay-gateway.ts` (live) | Create recurring subscription |
   | `ARBCancelSubscriptionRequest` | `lib/payments/authorize-net-arb.ts` <!-- Phase 7 PLB-06 --> | Cancel ARB subscription |
   | `ARBGetSubscriptionRequest` | `lib/payments/authorize-net-arb.ts` <!-- Phase 7 PLB-06 --> | Get subscription status / amount |
   | `ARBUpdateSubscriptionRequest` | `lib/payments/authorize-net-arb.ts` <!-- Phase 7 PLB-06 --> | Update next-cycle (NOT retro) |
   | `createCustomerProfileRequest` | `lib/payments/authorize-net-cim.ts` <!-- Phase 7 PLB-05 --> | Create CIM customer profile |
   | `createCustomerPaymentProfileRequest` | `lib/payments/authorize-net-cim.ts` <!-- Phase 7 PLB-05 --> | Add payment profile to customer |
   | `getCustomerProfileRequest` | `lib/payments/authorize-net-cim.ts` <!-- Phase 7 PLB-05 --> | Read CIM profile + payment profiles |
   | `updateCustomerPaymentProfileRequest` | `lib/payments/authorize-net-cim.ts` <!-- Phase 7 PLB-05 --> | Edit payment profile (Path A) |
   | `deleteCustomerProfileRequest` | `lib/payments/authorize-net-cim.ts` <!-- Phase 7 PLB-05 --> | Delete CIM profile |
   | `createTransactionRequest` (auth+capture) | `lib/payments/authorize-net-charges.ts` <!-- Phase 7 PLB-07 --> | One-shot charge of opaque token |
   | `createTransactionRequest` (refundTransaction) | `lib/payments/authorize-net-charges.ts` <!-- Phase 7 PLB-07 --> | Refund settled transaction |
   | `createTransactionRequest` (voidTransaction) | `lib/payments/authorize-net-charges.ts` <!-- Phase 7 PLB-07 --> | Void unsettled transaction |
   | `getTransactionDetailsRequest` | `lib/payments/authorize-net-charges.ts` <!-- Phase 7 PLB-07 --> | Lookup transaction status |
   | `authenticateTestRequest` | `instrumentation.ts` <!-- Phase 7 PLB-11 --> | Boot-time credential validation |

6. **Code patterns.** Heading: `## Code patterns`.

   - **gatewayFetch envelope (live, lib/payments/corepay-gateway.ts:50-89):** Show the implementation excerpt from interfaces above. Annotate the `merchantAuthentication` injection line with a comment.
   - **ARB create call (live, lib/payments/corepay-gateway.ts:95-147):** Show the call pattern: `gatewayFetch<SubscriptionResponse>({ ARBCreateSubscriptionRequest: { subscription } }, params.credentials)`. Note `paymentSchedule.startDate` semantics + `totalOccurrences: '9999'` for indefinite recurring.
   - **CIM create + ARB link (Phase 7, bracketed):**
     ```markdown
     <!-- Phase 7 work — not yet in codebase -->
     ```
     Then a typescript code fence showing: `createCustomerProfileRequest` → response gives `customerProfileId` + `customerPaymentProfileId` → pass those to `ARBCreateSubscriptionRequest.subscription.profile.{customerProfileId, customerPaymentProfileId}` instead of `subscription.payment.opaqueData`.
     Then close marker:
     ```markdown
     <!-- end Phase 7 work -->
     ```
   - **Webhook verification (Phase 7, bracketed):** Same HTML comment bracketing. Show:
     ```typescript
     // app/api/webhook/corepay/route.ts (Phase 7 PLB-09)
     export async function POST(request: NextRequest) {
       const rawBody = await request.text()  // MUST be first body read
       const signature = request.headers.get('x-anet-signature') || ''
       const ok = verifyCorepayWebhook(rawBody, signature, process.env.COREPAY_WEBHOOK_SECRET!)
       if (!ok) return new Response('invalid signature', { status: 401 })
       // ... idempotency lookup against webhook_events, then dispatch
     }
     ```

7. **When implementing a new Corepay call.** Heading: `## When implementing a new Corepay call`. Numbered list:

   1. **Use `gatewayFetch()` from `lib/payments/corepay-gateway.ts`.** Do not `fetch()` Authorize.Net directly — you'll forget `merchantAuthentication` and waste a sandbox request. Phase 7 helpers (`authorize-net-cim.ts`, `authorize-net-arb.ts`, `authorize-net-charges.ts`) extend the same helper.
   2. **Pass the request body keyed by operation name.** Example: `{ createCustomerProfileRequest: { profile, validationMode: 'liveMode' } }`. The top-level key MUST match the Authorize.Net operation name exactly.
   3. **Check `data.messages.resultCode === 'Ok'` before reading payload.** `gatewayFetch()` already throws on `'Error'`, but specific endpoints return useful sub-codes (e.g., E00040 = duplicate transaction).
   4. **Handle 4xx/5xx differently from `resultCode: 'Error'`.** HTTP errors mean the gateway is down or credentials wrong (Code 13). `resultCode: 'Error'` means the gateway accepted the request but rejected the operation (e.g., declined card). Map differently in admin UI.
   5. **Idempotency for charges via `refTransId` / external order ID.** Authorize.Net does not have a built-in idempotency key like Stripe; use `refTransId` on retries OR enforce a unique `(provider, event_id)` constraint in `webhook_events` (PLB-01) for incoming events.
   6. **Log without PHI.** Card last-4 is OK to log; full PAN, CVV, opaque token, or full billing address is not. (Authorize.Net never returns full PAN, but be careful with input echoes.)
   7. **Test against `apitest.authorize.net` first.** Boot-time `authenticateTestRequest` (PLB-11) catches sandbox-creds-in-prod / prod-creds-in-sandbox at startup. Run the smoke route `app/api/admin/corepay-smoke/route.ts` (Phase 7 PLB-13) for a full sandbox round-trip.

8. **CULTR integration patterns.** Heading: `## CULTR integration patterns`.
   - File layout (with bracket markers for Phase 7 files):
     - `lib/payments/corepay-gateway.ts` — gatewayFetch + ARB createSubscription (live)
     - `<!-- Phase 7 -->` `lib/payments/authorize-net-cim.ts` — CIM operations (PLB-05)
     - `<!-- Phase 7 -->` `lib/payments/authorize-net-arb.ts` — ARB cancel/get/update (PLB-06)
     - `<!-- Phase 7 -->` `lib/payments/authorize-net-charges.ts` — charges/refunds/voids (PLB-07)
     - `<!-- Phase 7 -->` `lib/payments/corepay-webhooks.ts` — `verifyCorepayWebhook(rawBody, headers, secret)` (PLB-08)
     - `<!-- Phase 7 -->` `app/api/webhook/corepay/route.ts` — webhook receiver (PLB-09)
     - `<!-- Phase 7 -->` `app/api/admin/corepay-smoke/route.ts` — sandbox smoke test (PLB-13)
   - Feature flag: `NEXT_PUBLIC_ENABLE_COREPAY=true` enables CorePay; FALSE everywhere as of 2026-04-28
   - DB columns: `memberships.provider_payment_profile_id`, `memberships.provider_subscription_id`, `memberships.provider_customer_id` (PLB-02)
   - Feature parity with Stripe is tracked in REQUIREMENTS.md PRT-01 through PRT-14 (Phase 10) and HRD-07/HRD-08 (Phase 13)

**File structure rules (D-09):**
- Single SKILL.md only — no `references/` subdirectory for this skill
- ~250 lines target, mirroring siphox-api/SKILL.md structure
- All HTML comment markers `<!-- Phase 7 ... -->` and `<!-- end Phase 7 work -->` must be present so spec-forward sections are visually distinct

**Reinforce CLAUDE.md HIPAA rules:** In the "When implementing" section, item 6 mandates HIPAA log discipline. Card last-4 OK; full PAN/CVV/opaque token NOT OK.

After writing the file, do NOT run any installation commands or modify other files. The .gitignore exception is handled in Plan 03.
  </action>

  <acceptance_criteria>
    - File exists: `[ -f .claude/skills/corepay-api/SKILL.md ] && echo present` returns "present"
    - Frontmatter present: `head -4 .claude/skills/corepay-api/SKILL.md | grep -c "^---$"` returns 2 (open + close fence)
    - Frontmatter name: `grep "^name: corepay-api" .claude/skills/corepay-api/SKILL.md` returns one match
    - Trigger keywords in description: `grep -E "COREPAY_TRANSACTION_KEY|NEXT_PUBLIC_COREPAY_API_LOGIN_ID|gatewayFetch|ARBCreateSubscriptionRequest|createCustomerProfile" .claude/skills/corepay-api/SKILL.md | head -1` returns a frontmatter line containing all five keywords (verify by reading lines 1-4 directly)
    - Vocabulary section present and FIRST: `grep -n "## Vocabulary" .claude/skills/corepay-api/SKILL.md` returns a line number, AND that line number is LESS than the line number returned by `grep -n "## Overview" .claude/skills/corepay-api/SKILL.md`
    - Three-company disambiguation in Vocabulary: `grep -c "Corepay" .claude/skills/corepay-api/SKILL.md` returns >= 5; `grep -c "Authorize.Net" .claude/skills/corepay-api/SKILL.md` returns >= 5; `grep -c "Corpay" .claude/skills/corepay-api/SKILL.md` returns >= 3 (Corepay-vs-Corpay disambiguation requires multiple uses)
    - URL trap reference: `grep "developer.crossborder.corpay.com" .claude/skills/corepay-api/SKILL.md` returns >= 1 match
    - authorizenet SDK gotcha with HMAC failure mode: `grep -A 3 "authorizenet" .claude/skills/corepay-api/SKILL.md | grep -i "HMAC\|signature\|webhook"` returns >= 1 match (proves failure mode is documented, not just the rule)
    - DO NOT install rule (NOT install): `grep -c "npm install authorizenet" .claude/skills/corepay-api/SKILL.md` may return 0 OR may match only when adjacent to "do not"/"NOT" prefix; verify by reading: every occurrence of "authorizenet" must be in a "do not install" / "NOT install" / "no SDK" context. Specifically `grep -B 1 -A 1 "authorizenet" .claude/skills/corepay-api/SKILL.md | grep -iE "do not|not install|no.*SDK|never|breaks"` returns >= 1 match
    - HMAC-SHA512 documented: `grep -i "HMAC-SHA512\|sha512" .claude/skills/corepay-api/SKILL.md` returns >= 1 match
    - request.text() for webhook: `grep "request.text()" .claude/skills/corepay-api/SKILL.md` returns >= 1 match
    - Accept.js 15-minute token gotcha: `grep -i "15.min\|fifteen.min\|single-use\|opaque.*token" .claude/skills/corepay-api/SKILL.md` returns >= 1 match
    - ARB next-cycle-only documented: `grep -iE "next.cycle|next cycle" .claude/skills/corepay-api/SKILL.md` returns >= 1 match
    - Code 13 / sandbox-vs-prod gotcha: `grep -i "Code 13\|sandbox.*prod\|prod.*sandbox" .claude/skills/corepay-api/SKILL.md` returns >= 1 match
    - IP allowlist documented: `grep -E "198\.241\.20[67]\.38" .claude/skills/corepay-api/SKILL.md` returns >= 2 matches (both IPs)
    - Spec-forward HTML comments present: `grep -c "Phase 7" .claude/skills/corepay-api/SKILL.md` returns >= 5 (multiple sections marked); `grep -c "<!--" .claude/skills/corepay-api/SKILL.md` returns >= 4
    - Five env vars documented: `grep -E "NEXT_PUBLIC_COREPAY_API_LOGIN_ID|COREPAY_TRANSACTION_KEY|NEXT_PUBLIC_COREPAY_PUBLIC_CLIENT_KEY|COREPAY_ENVIRONMENT|NEXT_PUBLIC_ENABLE_COREPAY" .claude/skills/corepay-api/SKILL.md | wc -l` returns >= 5
    - Section order: `grep -nE "^## (Vocabulary|Overview|Gotchas|Quick reference|Code patterns|When implementing|CULTR integration)" .claude/skills/corepay-api/SKILL.md | sort -t: -k1n` shows Vocabulary BEFORE Overview BEFORE Gotchas BEFORE Quick reference BEFORE Code patterns BEFORE When implementing BEFORE CULTR integration
    - File length sanity: `wc -l .claude/skills/corepay-api/SKILL.md | awk '{print $1}'` returns >= 200
    - No `references/` subdirectory created (D-09): `[ ! -d .claude/skills/corepay-api/references ] && echo single-file-only` returns "single-file-only"
  </acceptance_criteria>

  <verify>
    <automated>test -f .claude/skills/corepay-api/SKILL.md && grep -q "^name: corepay-api$" .claude/skills/corepay-api/SKILL.md && grep -q "developer.crossborder.corpay.com" .claude/skills/corepay-api/SKILL.md && grep -qE "198\.241\.20[67]\.38" .claude/skills/corepay-api/SKILL.md && grep -q "request.text()" .claude/skills/corepay-api/SKILL.md && grep -q "## Vocabulary" .claude/skills/corepay-api/SKILL.md && [ ! -d .claude/skills/corepay-api/references ] && echo "PASS"</automated>
  </verify>

  <done>
    File `.claude/skills/corepay-api/SKILL.md` exists with all sections in correct order, all 7 gotchas with failure modes, all 5 env vars listed, Vocabulary section before Overview, HTML comment markers around Phase-7-only sections, and no `references/` subdirectory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure documentation phase — no API endpoints, no auth changes, no PHI handling, no new dependencies installed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `.claude/skills/corepay-api/SKILL.md` accidentally embedding real credentials | mitigate | Skill documents env var NAMES only; never paste live values. Acceptance criteria explicitly check for env var names, not real keys. CLAUDE.md HIPAA rule reinforces. |
| T-06-02 | Tampering | Markdown file editable by anyone with repo access | accept | Standard repo-write trust boundary; skill is committed under code review. No more sensitive than CLAUDE.md itself. |
| T-06-03 | Repudiation | n/a | accept | Documentation file; git log provides history. |

**Threat surface assessment:** Documentation-only deliverable. No new API routes, no auth flows, no data persistence, no third-party calls at runtime. Threat surface is effectively nil; the only credential-disclosure risk is mitigated by the skill author following the "env var names only" rule documented in the skill itself.
</threat_model>

<verification>
Run the automated verify command. Confirm:
- File exists with correct frontmatter
- All 7 gotchas present
- Vocabulary section is FIRST content section
- All Phase-7 sections clearly marked with HTML comments
- All 5 env vars documented
- No `references/` subdirectory
</verification>

<success_criteria>
SKL-01 satisfied: `.claude/skills/corepay-api/SKILL.md` exists, is loadable by Claude Code via the frontmatter trigger, and provides spec-forward documentation that Phase 7 implementers can use without re-researching.
</success_criteria>

<output>
After completion, create `.planning/phases/06-skills/06-01-SUMMARY.md` describing the file written, the gotchas covered, the spec-forward sections marked, and a sanity check against `siphox-api/SKILL.md` for format consistency.
</output>
